{
  "name": "@poc-plattform-kit/marketing-oauth",
  "version": "0.3.0",
  "private": true,
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo \"lint:marketing-oauth — covered by root eslint on staged files\"",
    "test": "pnpm --filter @poc-plattform-kit/email run build && pnpm build && node --test dist/src/origins.spec.js dist/src/contact.spec.js",
    "start": "func start"
  },
  "dependencies": {
    "@azure/functions": "^4.6.0",
    "@poc-plattform-kit/email": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20"
  }
}
