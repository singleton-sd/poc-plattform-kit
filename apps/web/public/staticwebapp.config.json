{
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": [
      "/api/*",
      "/_next/*",
      "/assets/*",
      "/*.{css,js,ico,png,svg,webmanifest,txt,json,map}"
    ]
  },
  "globalHeaders": {
    "Cross-Origin-Opener-Policy": "same-origin-allow-popups"
  }
}
